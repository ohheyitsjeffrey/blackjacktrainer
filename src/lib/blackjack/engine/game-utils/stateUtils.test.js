import {
  createNewState,
  hasStateInLocalStorage,
  restoreState,
} from "./stateUtils";

beforeEach(() => {
  localStorage.clear();
});

// createNewState()
it("createNewState() returns default state", () => {

});

// hasStateInLocalStorage()
it("hasStateInLocalStorage() returns true if valid state stored in local storage", () => {

});

it("hasStateInLocalStorage() returns false if no valid state stored in local storage", () => {

});

it("hasStateInLocalStorage() returns false if incomplete state stored in local storage", () => {

});
